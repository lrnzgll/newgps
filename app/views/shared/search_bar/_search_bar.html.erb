<%= form_with url: "/search", html: { class: "search" } do |form| %>
  <%= form.text_field :text, placeholder: "cerca il tuo itineario", class: "searchTerm"%>
  <span class="searchButton">
    <%= form.submit%>
  </span>
<% end %>